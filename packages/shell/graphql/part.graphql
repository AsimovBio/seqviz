fragment PartFields on part {
  alias
  description
  id
  name
  sequence
  sequence_length
  part_type {
    glyph
    name
    slug
  }
}

query Parts {
  part(
    order_by: { part_type_id: desc }
    where: { part_type: { name: { _neq: "CDS" } } }
  ) {
    ...PartFields
  }
}
